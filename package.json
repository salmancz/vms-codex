{
  "name": "gate-operations-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "lint": "node --experimental-strip-types -e \"import('./apps/api/src/visits/visits.service.ts').then(()=>import('./apps/api/src/health/health.controller.ts'))\"",
    "prisma:validate": "prisma validate --schema packages/database/prisma/schema.prisma"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "prisma": "^5.19.1"
  },
  "type": "module"
}